"""
   `minibilles.daemon` runs any process a daemon givin the possibility to start, 
   stop and restart it with a simple command.
   
   Run `ceylon minibilles.daemon [options*] start|stop|restart [process] [process args*]`
   where:

   - TBD
   
   """
module minibilles.cli "0.1.0" {
	import ceylon.collection "1.2.0";	
}
